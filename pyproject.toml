[project]
name = "openems-zero-injection"
version = "0.7.0a3"
requires-python = ">=3.14.2"

[tool.pytest.ini_options]
pythonpath = ["."]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 88
target-version = "py314"
